<div class="dynamic-content">
            <div class="left">
                <h3>Oversigt:</h3>
                <ul>
                    <li th:text="'Indtjening pr/mnd: ' + ${totalIncome} + ',-'">
                    <li th:text="'Antal udlejede biler: ' + ${totalAmount}">

                </ul>
            </div>
            <div class="right">
                <div class="container">

                    <table>
                        <thead>
                        <tr>
                            <td>Stelnummer</td>
                            <td>Mærke</td>
                            <td>Model</td>
                            <td>Status</td>
                            <td>Pris pr/mnd</td>
                        </tr>
                        </thead>
                        <tbody>
                        <tr th:each="bil: ${bilList}">
                            <td th:text="${bil.stelnummer}">
                            <td th:text="${bil.mærke}">
                            <td th:text="${bil.model}">
                            <td th:text="${bil.bilStatus}">
                            <td th:text="${bil.leasingPrice}+',-'">
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>